# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/abgp_db

# JWT for login tokens (use a long random string in production)
JWT_SECRET=your-secret-change-in-production

# API port
PORT=3001
